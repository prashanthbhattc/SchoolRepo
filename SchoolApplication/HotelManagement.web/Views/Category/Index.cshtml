@using School.Entities

@model List<Category>
@{
    ViewBag.Title = "Index";

    var data1 = ViewData["category"] as List<Category>;
    var data2 = ViewBag.Mydata as List<Category>;
}

<h2>Categories</h2>

@*<table class="table table-bordered">
        <thead>
            <tr>

                <th scope="col">Id</th>
                <th scope="col">Name</th>
                <th scope="col">Action</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in data1)
            {
            <tr>

                <td>@item.Id</td>
                <td>@item.Name</td>
                <td>
                    @Html.ActionLink("Edit", actionName: "edit", controllerName: "Category", routeValues: new { id = item.Id }, htmlAttributes: new { @class = "btn btn-primary" })
                </td>
                    </tr>

            }

        </tbody>
    </table>

    <hr/>



    <table class="table table-bordered">
        <thead>
            <tr>

                <th scope="col">Id</th>
                <th scope="col">Name</th>
                <th scope="col">Action</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in data2)
            {
                <tr>

                    <td>@item.Id</td>
                    <td>@item.Name</td>
                    <td>
                        @Html.ActionLink("Edit",actionName:"edit",controllerName:"Category",routeValues:new {id=item.Id },htmlAttributes:new {@class="btn btn-primary" })

                    </td>
                </tr>

            }

        </tbody>
    </table>*@
<a class="btn btn-primary" href="/Category/Add">Create new</a>
@Html.ActionLink("Create", actionName: "Add", controllerName: "Category",routeValues: null,  htmlAttributes: new { @class = "btn btn-primary" })
<table class="table table-bordered">
    <thead>
        <tr>

            <th scope="col">Id</th>
            <th scope="col">Name</th>
            <th scope="col">Action</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>

                <td>@item.Id</td>
                <td>@item.Name</td>
                <td>
                    @Html.ActionLink("Edit", actionName: "edit", controllerName: "Category", routeValues: new { id = item.Id }, htmlAttributes: new { @class = "btn btn-primary" })
                </td>
            </tr>

        }

    </tbody>
</table>