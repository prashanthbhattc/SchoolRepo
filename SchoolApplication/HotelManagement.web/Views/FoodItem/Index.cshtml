
@using School.Entities

@model List<FoodItem>

@{
    ViewBag.Title = "Index";

    var data1 = ViewData["fooditem"] as List<FoodItem>;
    var data2 = ViewBag.Mydata as List<FoodItem>;
    var data3 = ViewBag.Mydata as List<Category>;

}

<h2>fooditems</h2>
<h2>category</h2>


@Html.ActionLink("Create", actionName: "Add", controllerName: "FoodItem", routeValues: null, htmlAttributes: new { @class = "btn btn-primary" })

<table class="table table-bordered">
    <thead>
        <tr>

            <th scope="col">Id</th>
            <th scope="col">Name</th>
            <th scope="col">CategoryId</th>
            <th scope="col">CategoryName</th>
            <th scope="col">Action</th>

        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
        <tr>

            <td>@item.Id</td>
            <td>@item.Name</td>
            <td>@item.category.Id</td>
            <td>@item.category.Name</td>

            <td>
                @Html.ActionLink("Edit", actionName: "edit", controllerName: "FoodItem", routeValues: new { id = item.Id }, htmlAttributes: new { @class = "btn btn-primary" })


            </td>
        </tr>

        }

    </tbody>
</table>

